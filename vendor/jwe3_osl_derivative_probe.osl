shader JWE3_OSL_Derivative_Probe(
    float Scale = 1.0,
    output color Color = color(0.0))
{
    float value = P[0] * Scale;
    float dx = Dx(value);
    float dy = Dy(value);
    Color = color(abs(dx), abs(dy), 0.0);
}
